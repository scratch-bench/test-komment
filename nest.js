/**
 * @description simply returns a value of 0
 * 
 * @returns { integer } 0.
 */
const X = () => {
  /**
   * @description returns  0, indicating that it does not perform any action or produce
   * any output.
   * 
   * @returns { integer } `0`.
   */
  const Y = () => {
    return 0;
  }

  return Y();
}
