/**
 * @description returns a value of 0.
 * 
 * @returns { integer } `0`.
 */
const X = () => {
  /**
   * @description returns  0
   * 
   * @returns { number } 0.
   */
  const Y = () => {
    return 0;
  }

  return Y();
}
