/**
 * already documented
 */
function test() {
    return true;
}

/**
* @description The function `other()` does nothing (has no implementation) and will 
* result of `undefined` when called.
* 
* 
* 
* @returns {  } The output of the `other()` function is `undefined`.
*/
function other() {
}

class Ticker {
/**
* @description The function constructs a new object and sets the "count" property 
* to 0.
* 
* 
* 
* @returns { number } The output returned by this function is undefined.
*/
    constructor() {
        this.count = 0;
    }
}

/**
 * also doced
 */
function fail() {
    return false;
}
